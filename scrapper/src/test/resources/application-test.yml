app:
  scrapper:
    scheduler:
      interval: 15s
      force-check-delay: 30s
    github:
      token: github-token
      url: https://example.ru
    stackoverflow:
      url: https://api.stackexchange.com/2.3
      key: stackoverflow-key
      access-token: stackoverflow-access-token
    bot-url: https://example.eu
  database:
    access-type: ORM
